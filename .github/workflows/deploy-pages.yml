name: Deploy Landing To GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Install dependencies
        run: |
          npm ci
          ROLLUP_VERSION=$(node -p "require('./package-lock.json').packages['node_modules/rollup'].version")
          SWC_VERSION=$(node -p "require('./package-lock.json').packages['node_modules/@swc/core'].version")
          npm install --no-save "@rollup/rollup-linux-x64-gnu@${ROLLUP_VERSION}" "@swc/core-linux-x64-gnu@${SWC_VERSION}"

      - name: Verify native build dependencies
        run: |
          node -e "require.resolve('@rollup/rollup-linux-x64-gnu')"
          node -e "require('@swc/core')"

      - name: Build landing for Pages base path
        env:
          ATOLL_LANDING_BASE: /${{ github.event.repository.name }}/
        run: npm run build:landing

      - name: Add SPA fallback
        run: cp landing/dist/index.html landing/dist/404.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: landing/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
